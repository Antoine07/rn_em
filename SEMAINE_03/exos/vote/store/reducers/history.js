import { SET_MEMORY } from "../constants/actions";

const stateInit = {
    choices: []
};

const reducer = (state = stateInit, action = {}) => {
    switch (action.type) {

        default:
            return state;
    }
};

export default reducer;